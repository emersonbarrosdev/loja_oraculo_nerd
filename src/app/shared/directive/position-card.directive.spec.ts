import { PositionCardDirective } from './position-card.directive';

describe('PositionCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionCardDirective();
    expect(directive).toBeTruthy();
  });
});
